let loadingRemoved=false;const params=new URLSearchParams(location.search);const id=(params.get('lesson')||'01').padStart(2,'0');const total=({'01':9,'02':12,'03':12})[id]||8;const player=document.querySelector('#player');let current=0;const slides=Array.from({length:total},(_,i)=>{const n=String(i+1).padStart(2,'0');const img=document.createElement('img');img.className='slide';img.dataset.src=`/class/ppt-preview/${id}/slide-${n}.png`;img.alt=`第 ${id} 堂第 ${i+1} 張投影片`;img.loading='lazy';img.decoding='async';img.draggable=false;player.appendChild(img);return img});const top=document.createElement('div');top.className='topbar';top.innerHTML=`<a class="back" href="/class/lesson-${id}.html">← 回到課程頁</a><span class="counter"></span>`;player.appendChild(top);const controls=document.createElement('div');controls.className='controls';controls.innerHTML='<button data-action="prev" aria-label="上一張">←</button><button class="primary" data-action="full">全螢幕</button><button data-action="next" aria-label="下一張">→</button>';player.appendChild(controls);const hint=document.createElement('div');hint.className='hint';hint.textContent='左右鍵／空白鍵翻頁・手機左右滑動・按 F 進入全螢幕';player.appendChild(hint);function load(i,priority=false){const img=slides[(i+total)%total];if(!img.src){img.src=img.dataset.src;if(priority)img.fetchPriority='high'}}function show(n){if(!loadingRemoved){document.querySelectorAll('.loading').forEach(e=>e.remove());loadingRemoved=true;}current=(n+total)%total;load(current,true);load(current+1);load(current-1);slides.forEach((s,i)=>s.classList.toggle('active',i===current));top.querySelector('.counter').textContent=`第 ${current+1} / ${total} 張`;document.title=`第 ${id} 堂｜第 ${current+1} 張｜CALUMAI`}function next(){show(current+1)}function prev(){show(current-1)}function fullscreen(){if(!document.fullscreenElement)document.documentElement.requestFullscreen?.();else document.exitFullscreen?.()}controls.addEventListener('click',e=>{const a=e.target.dataset.action;if(a==='next')next();if(a==='prev')prev();if(a==='full')fullscreen()});document.addEventListener('keydown',e=>{if(['ArrowRight',' ','PageDown'].includes(e.key)){e.preventDefault();next()}if(['ArrowLeft','PageUp'].includes(e.key)){e.preventDefault();prev()}if(e.key.toLowerCase()==='f')fullscreen();if(e.key==='Escape'&&document.fullscreenElement)document.exitFullscreen?.()});let downX=0;document.addEventListener('touchstart',e=>downX=e.changedTouches[0].clientX,{passive:true});document.addEventListener('touchend',e=>{const dx=e.changedTouches[0].clientX-downX;if(Math.abs(dx)>45)(dx<0?next:prev)()},{passive:true});show(0);
